# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# esbuild bundles for `api`/`worker` (pnpm build:bundles) — generated by the
# Docker builder, don't belong in the repo.
out

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# The project uses pnpm (see the packageManager field and the preinstall guard).
# pnpm-lock.yaml is committed; don't track the byproduct of an accidental `npm install`.
package-lock.json

.vercel
