# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Az `api`/`worker` esbuild bundle-jei (pnpm build:bundles) — a Docker builder
# generálja őket, a repóba nem valók.
out

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# A projekt pnpm-et használ (lásd a packageManager mezőt és a preinstall guardot).
# A pnpm-lock.yaml commitolva van; egy véletlen `npm install` termékét ne kövessük.
package-lock.json

.vercel
