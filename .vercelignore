# The Vercel deploy only needs the frontend + the api/ handlers. The Docker/Pi
# side of things would needlessly bloat the deploy size and build time.
server
worker
scripts
docker
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.override.yml
.env.docker.example
